{
  "compilerOptions": {
    "composite": true,
    "rootDir": "."
  },
  // excluded from build
  "exclude": ["src/**/*.ts"],
  "extends": "./tsconfig.base.json",
  "files": ["package.json"],
  // included for IDE support
  "include": ["src/**/*.ts"]
}
